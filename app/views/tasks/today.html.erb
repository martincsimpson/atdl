<h1>Today</h1>

<p> This page is for tasks that need to be done, or reviewed today </p>

<% @workspaces.each do |workspace| %>
  <% if workspace.any_task_matching?(:today) %>
    <%= render partial: 'workspaces/workspace', locals: { workspace: workspace, tasks_scope: @tasks_scope } %>
  <% end %>
<% end %>


<div class="recurring_tasks">
  <h2>Recurring Tasks</h2>
<% if @recurring_tasks.any? %>
<% @recurring_tasks.each do |recurring_task| %>
  <%= render partial: 'recurring_tasks/recurring_task', locals: { recurring_task: recurring_task } %>
<% end %>
<% else %>
Congrats, all complete!
<% end %>
</div>