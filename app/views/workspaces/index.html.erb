<h1>Workspaces</h1>

<% @workspaces.each do |workspace| %>
  <%= render partial: 'workspaces/workspace', locals: { workspace: workspace } %>
<% end %>

<%= link_to 'New Workspace', new_workspace_path %>